#include "text.h"


void Text::print(SDL_Renderer* renderer,  char string, int x, int y) {
  draw_rect.x = x;
  draw_rect.y = y;
  
  for (auto& letter : string) {
    printf(letter);
  }

  SDL_RenderFillRect(renderer, &draw_rect); 
}
